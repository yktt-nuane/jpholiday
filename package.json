{
    "name": "jp-holiday-calendar",
    "version": "1.0.0",
    "description": "Japanese holiday calendar AWS CDK project",
    "scripts": {
        "prepare": "husky install",
        "lint": "ruff check . && ruff format --check . && mypy .",
        "format": "black . && isort . && ruff format .",
        "test": "pytest"
    },
    "devDependencies": {
        "husky": "^8.0.3"
    }
}